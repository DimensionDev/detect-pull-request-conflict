{
  "name": "@dimensiondev/detect-conflict",
  "private": true,
  "scripts": {
    "build": "rimraf dist && tsc",
    "build:aws-lambda": "./scripts/build-aws-lambda.sh",
    "deploy": "serverless deploy",
    "start": "ts-node scripts/run-probot.ts"
  },
  "dependencies": {
    "@probot/adapter-aws-lambda-serverless": "^3.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.33.1",
    "octokit": "^1.7.1",
    "prettier": "^2.5.1",
    "probot": "^12.1.3",
    "rimraf": "^3.0.2",
    "serverless": "^2.70.0",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  }
}
